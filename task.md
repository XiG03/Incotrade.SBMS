# Phase 1: Foundation & Setup

- [x] Initialize standard folder structure (`src/` based).
- [x] Install dependencies (`axios`, `zustand`, `react-query`, `react-hook-form`, `zod`, `lucide-react`, `clsx`, `tailwind-merge`).
- [x] Configure Axios instance with base URL and generic interceptors (`src/lib/axios.ts`).
- [x] Define TypeScript interfaces based on the .NET `DTOs` (`src/types/`).
- [x] Create utility functions (`src/lib/utils.ts`).
- [x] Convert global UI assets and CSS from the layout folder to `src/app/globals.css`.
